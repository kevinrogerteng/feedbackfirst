<div class="totaltickets">
  <h1>{{tickets.length}}</h1>
</div>
<div class="filterPosts">
  Some filter stuff here
</div>
<div ng-repeat="post in posts">
  <div class="post">
    <h3>{{post.tickets.length}}</h3>
    <h2><a href="/posts/{{post.id}}">{{post.title}}</a></h2>
  </div>
</div>

